# Multi-page delivery regression

## Page one

multi-page-marker-2026 begins a report with enough independent paragraphs to exercise page breaks and repeated layout rules.

This paragraph contains a stable sentence for PDF text extraction. It should not disappear when the document crosses a page boundary.

## Nested headings

### Level three

The heading must stay attached to the following paragraph and must not be orphaned by the renderer.

#### Level four

The fourth-level heading is intentionally included for heading hierarchy coverage.

## Repeated content

Paragraph 01: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 02: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 03: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 04: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 05: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 06: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 07: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 08: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 09: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 10: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 11: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 12: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 13: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 14: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 15: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 16: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 17: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 18: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 19: The delivery artifact remains Markdown-first while PDF is an optional rendered output.

Paragraph 20: The delivery artifact remains Markdown-first while PDF is an optional rendered output.
